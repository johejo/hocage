hooks:
  bad_hook:
    event_name: PreToolUse
    transcript:
      load: true
    when: "true"
    action:
      respond:
        decision: "block"
    tests:
      bad_jsonl:
        transcript: |
          {this is not valid json}
        inputs:
          - {}
