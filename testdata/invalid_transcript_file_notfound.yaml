hooks:
  bad_hook:
    event_name: PreToolUse
    transcript:
      load: true
    when: "true"
    action:
      respond:
        decision: "block"
    tests:
      bad_file:
        transcript_file: testdata/nonexistent.jsonl
        inputs:
          - {}
